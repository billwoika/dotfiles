# linux/

Linux-specific opt-in helpers, parallel to `macos/`.

The core framework (bootstrap, zsh conf.d, profile, git, SSH, tmux)
works identically on macOS and Linux. This directory holds scripts
for Linux-only setup tasks that have no macOS equivalent or require
different tooling.
